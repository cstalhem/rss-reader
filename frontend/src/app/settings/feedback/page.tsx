import { SettingsPlaceholder } from "@/components/settings-placeholder";

export default function FeedbackSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Feedback"
      blurb="Send a note about what's working and what isn't."
    />
  );
}
