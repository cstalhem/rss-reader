export default function CategoriesSettingsPage() {
  return (
    <div className="flex flex-col gap-5">
      <h2 className="text-lg font-semibold">Categories</h2>
      {/*
        TODO(#98 phase 4): mount the real Categories triage/manage UI here
        (Triage/Manage tabs backed by useCategories) — see the approved design
        in app/prototype/categories/categories-section.tsx for reference, but
        rebuild it against real data rather than importing the prototype.
      */}
    </div>
  );
}
