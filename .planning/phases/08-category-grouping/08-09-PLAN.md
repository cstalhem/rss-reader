---
phase: 08-category-grouping
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/settings/InterestsSection.tsx
  - frontend/src/components/settings/OllamaSection.tsx
  - frontend/src/components/settings/CategoriesSection.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All settings section subheaders use identical visual styling"
  artifacts:
    - path: "frontend/src/components/settings/InterestsSection.tsx"
      provides: "Panel subheaders with lg semibold styling"
      pattern: 'fontSize="lg".*fontWeight="semibold"'
    - path: "frontend/src/components/settings/OllamaSection.tsx"
      provides: "Panel subheaders with lg semibold styling"
      pattern: 'fontSize="lg".*fontWeight="semibold"'
    - path: "frontend/src/components/settings/CategoriesSection.tsx"
      provides: "Section heading with consistent styling"
      pattern: 'fontSize="lg".*fontWeight="semibold"'
  key_links: []
---

<objective>
Standardize all settings section subheaders to use the same visual pattern (fontSize=lg, fontWeight=semibold) for consistent design across Interests, Ollama, and Categories sections.

Purpose: Eliminate duplicate styling patterns and provide unified visual hierarchy across all settings panels
Output: Consistent subheader styling throughout settings page
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-category-grouping/08-06-SUMMARY.md
@.planning/phases/08-category-grouping/08-UAT.md

@frontend/src/components/settings/InterestsSection.tsx
@frontend/src/components/settings/OllamaSection.tsx
@frontend/src/components/settings/CategoriesSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Standardize subheader styling across all settings sections</name>
  <files>
    frontend/src/components/settings/InterestsSection.tsx
    frontend/src/components/settings/OllamaSection.tsx
    frontend/src/components/settings/CategoriesSection.tsx
  </files>
  <action>
Update all panel subheaders to use the same pattern: fontSize="lg", fontWeight="semibold", mb={4}

**InterestsSection.tsx lines 85-93 and 107-114:**
Change from:
```tsx
<Text
  fontSize="sm"
  fontWeight="semibold"
  color="fg.muted"
  textTransform="uppercase"
  letterSpacing="wider"
  mb={2}
>
```

To:
```tsx
<Text fontSize="lg" fontWeight="semibold" mb={4}>
```

**CategoriesSection.tsx (find similar patterns):**
Update any section subheaders to match the lg+semibold pattern if they don't already.

**OllamaSection.tsx:**
Already uses fontSize="lg" fontWeight="semibold" mb={4} pattern (lines 112, 129 visible in context). Keep as-is or verify consistency.

Gap reason from UAT: "Subheaders does not follow the same design as in the Ollama section. We want all headings, subheadings, labels, etc, in the settings to have the same design and not contain duplicate styling"

Standard pattern (from OllamaSection): lg + semibold + mb=4
Remove: uppercase, letterSpacing, smaller fontSize, color overrides on subheaders
  </action>
  <verify>
```bash
cd frontend && bun run build
```
Should build successfully. Visually verify in dev server that:
- Interests section "Interests" and "Anti-interests" subheaders are larger and not uppercase
- All panel subheaders across settings sections have matching visual appearance
  </verify>
  <done>
All settings section subheaders use fontSize="lg" fontWeight="semibold" mb={4} with no uppercase transform or custom letter spacing. Styling is consistent across Interests, Ollama, and Categories sections.
  </done>
</task>

</tasks>

<verification>
Build passes, no TypeScript errors. All subheaders in settings page use the same visual pattern.
</verification>

<success_criteria>
- InterestsSection panel subheaders match OllamaSection visual style
- No duplicate styling patterns exist across settings sections
- UAT test #2 passes with standardized subheader design
</success_criteria>

<output>
After completion, create `.planning/phases/08-category-grouping/08-09-SUMMARY.md`
</output>
